# Personal homepage

GitHub Pages personal homepage.

Local structure:

- `index.html` — homepage content
- `assets/css/style.css` — visual style
- `assets/js/main.js` — small navigation interaction
- `scripts/update_scholar_metrics.py` — updates Google Scholar metrics in `index.html`
- `.github/workflows/update-scholar-metrics.yml` — daily scheduled metric refresh
